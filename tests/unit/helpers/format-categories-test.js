import {
  formatCategories
} from '../../../helpers/format-categories';
import { module, test } from 'qunit';

module('FormatCategoriesHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatCategories(42);
  assert.ok(result);
});
