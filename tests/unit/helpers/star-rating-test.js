import {
  starRating
} from '../../../helpers/star-rating';
import { module, test } from 'qunit';

module('StarRatingHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = starRating(42);
  assert.ok(result);
});
